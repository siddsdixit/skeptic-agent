{
  "name": "skeptic-demo",
  "version": "1.0.0",
  "description": "Remotion-rendered hero demo for skeptic-agent",
  "type": "module",
  "private": true,
  "scripts": {
    "start": "remotion studio",
    "build": "remotion render SkepticDemo out/demo.mp4 --codec h264 --crf 18",
    "build:gif": "remotion render SkepticDemo out/demo.mp4 --codec h264 --crf 16 && rm -rf out/frames && mkdir -p out/frames && ffmpeg -y -i out/demo.mp4 -vf 'fps=18,scale=960:-1:flags=lanczos' out/frames/%04d.png && gifski out/frames/*.png -o ../assets/demo.gif --width 960 --fps 18 --quality 70",
    "preview": "remotion preview"
  },
  "dependencies": {
    "@remotion/cli": "^4.0.0",
    "@remotion/google-fonts": "^4.0.452",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "remotion": "^4.0.0"
  },
  "devDependencies": {
    "@types/react": "^19.0.0",
    "typescript": "^5.0.0"
  }
}
